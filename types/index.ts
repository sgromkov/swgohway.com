export type AlignmentCodeValues = 'light' | 'dark';

export interface IAlignmentSelectOption {
    title: string,
    code: string
}

export interface IFeatureSelectOption {
    title: string,
    code: string
}

export interface IFractionSelectOption {
    title: string,
    code: string
}

export interface IRoleSelectOption {
    title: string,
    code: string
}
